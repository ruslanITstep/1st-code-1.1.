#include <iostream>
using namespace std;


int main() {
	int* z = new int;
	cin >> *z;
	if (*z > -1 * (*z)) {
		cout << *z << endl;
	
	}
	else {
		cout <<-2 * (*z) / 2 << endl;
	}
	delete z;
	z = 0;
  
}
